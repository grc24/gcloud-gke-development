name: 'Terraform'

on: push

env:
  TF_LOG: INFO
  TF_INPUT: false
  TF_TOKEN: ${{ secrets.TF_TOKEN }}

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest

    # Use the Bash shell regardless whether the GitHub Actions runner is 
    # ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash

    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Checkout
      uses: actions/checkout@v3

    # Install the preferred version of Terraform CLI 
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2

    - name: Configure Terraform credentials
      run: |
        mkdir -p ~/.terraform.d
        cat > ~/.terraform.d/credentials.tfrc.json <<EOF
        {
          "credentials": {
            "app.terraform.io": {
              "token": "${{ secrets.TF_TOKEN }}"
            }
          }
        }
        EOF

    - name: Terraform Init
      id: init
      # Initialize Terraform with no input
      # This will download the required providers and modules
      # It will also create the .terraform directory
      run: terraform init -input=false


    # Run a terraform fmt for push
    - name: Terraform Format
      id: fmt
      run: terraform fmt -check

    # Run a terraform validate
    # Run even if formatting fails
    - name: Terraform Validate
      id: validate
      if: (success() || failure())
      run: terraform validate
